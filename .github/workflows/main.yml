name: devops-main
on:
  push:
    branches:
      - develop
  workflow_dispatch:
    inputs:
      tag:
        description: "Tag for the Docker image" # Descripción del input
        required: true # Hace que este campo sea obligatorio
        default: "latest"

# jobs:
#   echo-job:
#     runs-on: ubuntu-latest
#     environment:
#       name: devops
#     steps:
#       - name: Print environment variable
#         run: echo "HUBB_USER=${{ vars.HUBB_USER }}"

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    environment:
      name: devops
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - run: ls -la
      - run: docker --version

      - name: Build docker image
        run: docker build -t i004-wander-back-nodejs:${{ inputs.tag }} . #${{ github.event.inputs.tag }}

      - name: ls images docker
        run: docker images

      # - name: Login to Docker hub
      #   uses: docker/login-action@v3
      #   with:
      #     username: ${{ secrets.DOCKERHUB_USERNAME }}
      #     password: ${{ secrets.DOCKERHUB_TOKEN }}
